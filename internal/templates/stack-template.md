# Stack Template (retired)

Retired: the knowledge base no longer publishes separate per-path stack pages. The layered model lives on `docs/concepts/how-the-stack-fits-together.md`; each guide carries its own component map; operating assumptions live in the environment profiles (`docs/components/environments/`).

For a new path, use the guide template's component map plus an environment profile instead of a standalone stack page.
